%h1 Profile

%h2=@user.full_name(true)

%h4= @user.email

%p
  -@user.tags_array.each do |tag|
    %span.label.label-default=tag

-if current_user.admin or @user.id==current_user.id
  %p= link_to "Edit profile", edit_user_path(@user.id), :class=>"btn btn-default"