%h2 History of raports



%h4  
  
  -raport=@raports.where(user_id: current_user.id.to_s)
  -raport.each do |r|
    from 
    =r.date_begin 
    to 
    =r.date_end
    =link_to "link", root_url #tu bedzie link do konkretnego raportu
    %br

